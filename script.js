document.querySelectorAll('img[src^="assets/"]').forEach(img=>{img.addEventListener('error',()=>{img.closest('.photo,.offer-photo')?.classList.add('media-missing');img.remove()},{once:true})});const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('in')}),{threshold:.12});document.querySelectorAll('.hero-inner,.intro .wrap,.copy,.topics article,.experience>div,.result-grid article,.schedule-list article,.offer-box,.faq .wrap,.closing>div').forEach(el=>{el.classList.add('reveal');io.observe(el)});document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth'})}}));